server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # This is critical for Vue Router to work —
    # without it, refreshing any page other than / returns 404
    location / {
        try_files $uri $uri/ /index.html;
    }
}